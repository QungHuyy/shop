import { useEffect } from 'react';
import { router } from 'expo-router';

export default function TabCartRedirect() {
  useEffect(() => {
    // Redirect to standalone cart immediately
    router.replace('/cart');
  }, []);

  // Return null as this component only redirects
  return null;
} 